import { PayableList } from '@/components/pages/payable/payable.list'
import { RouteObject } from 'react-router-dom'

const routes: RouteObject[] = [{ path: 'payables', element: <PayableList /> }]

export default routes
